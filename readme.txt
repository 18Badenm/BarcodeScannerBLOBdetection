These files are my prototype for using BLOB detection in order to find barcodes and read the data into a terminal. just use the command python (file) and it should work. 

Deep is a sobel vector implementation the other one is a sobel vector and Blob detection algorithm
